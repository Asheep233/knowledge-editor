# v1.2.0 计划与已拍板决策

> 主理人 2026-09-18 定版：三项优化直接作 **1.2.0**，分三步走。本文件是**唯一计划入口**；
> 可行性分析原件见 `docs/analysis-1.1.10/`（`shortcuts.md` / `math-cursor.md` / `source-mode.md`），
> 旧文件 `docs/design-1.1.10-scope.md` 中的版本号已作废（内容仍可参考，版本以本文件为准）。

## 1. 三步走

| 步骤 | 内容 | 出口标准 |
|---|---|---|
| **1.2.0-pre.1** | 项① 自定义快捷键 + 项② 公式光标/换行 + G-1/G-2/G-3 小修 + **F-1…F-5 保真缺陷全部修复** | 全量回归（pytest / tsc / vitest / cargo）+ 每项独立对抗验证 PASS → bump `1.2.0-pre.1` → 预发布（侧车重建 + NSIS + manifest） |
| **1.2.0-pre.2** | **源码模式**（可编辑 + 字符串直存 + 单视图排他） | 规范先行（`document-format.md` 新增「编辑通道与视图」章节）→ 实现 → 独立对抗验证 → bump `1.2.0-pre.2` → 预发布 |
| **1.2.0 正式** | **大规模全量审查（不由 Lead 做）** → 修复审查发现 → 发布 | 审查通过 + 修订回归全绿 → bump `1.2.0` → 正式发布（Latest） |

## 2. 主理人已拍板（2026-09-18，共 11 条）

| # | 问题 | 裁决 |
|---|---|---|
| 1 | 快捷键与既有键位的关系 | **已有的快捷键不变动**；在设置里**新增自定义功能**（不改任何既有默认绑定） |
| 2 | F-1…F-5 保真缺陷 | **全部修** |
| 3 | 源码模式下未知/损坏语法的所有权 | **允许用户改写并保存**，但**保存时需加提示** |
| 4 | 「切换文档」的入口 | **做 Tab 栏**（文档标签页） |
| 5 | 块级公式后已有正文段落时的光标落点 | 落到**既有段落行首**（不插空行） |
| 6 | 快捷键作用域 | **全局** |
| 7 | 源码模式下 frontmatter | **隐藏**（不可见/不可编辑） |
| 8a | 切视图时未落盘编辑 | **自动保存**（切视图前 flush，与既有切文档行为一致） |
| 8b | 视图记忆粒度 | **全局**（一个开关，所有文档一致；不写进 `.md`） |
| 9 | 源码模式大文档编辑器 | **`<textarea>`**（零依赖，不上 CodeMirror） |
| — | `Ctrl+N` 双重绑定（菜单「新建文档」vs 编辑器「插入行内公式」） | 按第 1 条**不改任何既有绑定** → 作为**已知冲突**记入「保留键」清单 + 设置页提示；若日后要让编辑器侧可用，需另开裁决换键 |

## 3. 由裁决派生的实现约束

- **项①**：默认映射为空（全部沿用现状）；设置三处同步（`settings.ts` → IPC → `settings.rs` 结构体 + `from_value_lenient` 白名单 + `sanitize`）；深合并 → 解绑用**墓碑值**；补 2 条「三处不同步」守门测试；保留键（原生菜单 `Ctrl+N/O/,/Q/R`、`F12`）不可绑定或明确警告。
- **项②**：规范已先行（`design-v1.1.7-math-editor.md` §3.2 已按第 5 条重拍）；只读态**必须**加 `isEditable` 闸门（既有漏洞：`isEditable=false` 时 PM 命令仍会改文档）。
- **F-1…F-5**：需先补规范条款（换行策略 / BOM 容忍 / 行内 HTML 与实体保真 / 任务列表语法归属），再改代码；修改点集中在 tokenizer / 序列化 / `stripFrontmatter`。
- **Tab 栏**（第 4 条）：新增 App 层「打开文档集合」状态 + `EditorToolbar` 既有 `tabBar` 插槽；需覆盖脏态、切换前 flush（复用 `docSwitch` 缝）、关闭、顺序、与快捷键 `doc.next/prev` 集成。
- **源码模式**（pre.2）：字符串直存**完全绕开 ProseMirror**；**单视图排他**；切回正文时提示「原文中的未知语法可能被规范化」；frontmatter 隐藏；`<textarea>`；视图态**全局**；切换前自动 flush。

## 4. 任务板映射（pre.1）

| 任务 | 内容 | 归属 |
|---|---|---|
| task-28 | 项② 公式光标/换行 | dev-trash-fe |
| task-29 | 项① 自定义快捷键（设置三处同步 + 全局 capture 分发器） | dev-attach-ui |
| task-30 | Tab 栏（文档标签页） | 待派（避免与 ① 抢 `App.tsx`） |
| task-31 | F-1…F-5 保真修复 + G-1/G-2/G-3 小修 | Lead |
| task-32 | 独立验证：② + F-1…F-5 | verifier-s2 |
| task-33 | 独立验证：① + Tab 栏 | verifier-attach |
