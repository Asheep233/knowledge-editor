# v1.1.8 前置全量测试计划（文件管理 · 文件安全 · 编辑体验）

> 版本：draft-1（2026-09-08）· 执行者：主 Agent（自动化）+ 主理人（GUI 验收）
> 目标：在 v1.1.8 开发前，对三大域做一次**系统性全量回归 + 破坏性场景验证**，
> 产出可复现的缺陷清单与基线报告，作为 v1.1.8 的输入。
> 红线：**测试数据与用户真实工作区隔离**（见 §2）。

---

## 1. 范围

| 域 | 覆盖 | 不覆盖（明确排除） |
|---|---|---|
| A 文件管理 | 文档/文件夹 CRUD、树刷新、命名与 slug、嵌套、重命名、移动、附件、导出、快照/历史、工作区切换、最近列表、索引一致性 | 云同步、多设备协作 |
| B 文件安全 | 原子写、崩溃恢复、外部修改检测、并发冲突、路径穿越、符号链接/Junction、权限/只读、编码、大文件、autosave 落盘、关窗 flush、备份保留、frontmatter 合并 | 加密存储、权限模型（多用户） |
| C 编辑体验 | Markdown 往返保真、列表/公式/信息块/表格/脚注/图片、粘贴、撤销重做、快捷键、占位符、主题、设置持久化、保存状态、性能 | 协同编辑、移动端 |

## 2. 环境与数据隔离（硬性）

| 项 | 规约 |
|---|---|
| 测试工作区 | `%USERPROFILE%\Documents\KE-TestWorkspace`（专用，**绝不在真实工作区做破坏性用例**）|
| 夹具生成 | 脚本化生成：50 篇文档（含中文/数字/空格标题）、5 层嵌套目录、10 个空目录、附件 3 类（png/pdf/bin）、1 篇 256KB 大文档、1 篇非 UTF-8 文件、1 篇纯 frontmatter |
| 应用实例 | 开发版 exe（`target/release/knowledgeeditor.exe`，CDP 9222）；测试前 `taskkill` 清场 |
| 备份 | 每轮破坏性用例前对测试工作区做 `robocopy` 快照，用例后校验/恢复 |
| 生产验证 | 仅做**只读**冒烟（打开真实工作区看渲染/列表），不写入 |

## 3. 自动化基线（已存在，作为回归门槛）

| 套件 | 命令 | 当前基线 | 门槛 |
|---|---|---|---|
| 前端单测/往返 | `npx vitest run` | 244（243 pass + 1 skip）| 全绿，往返测试 0 失败 |
| 后端 | `python -m pytest` | 174 passed | 全绿 |
| Rust | `cargo test` | 13 passed | 全绿 |
| 类型/构建 | `tsc -b --noEmit` / `npm run build` | 0 错 | 0 错 |

---

## 4. A 域：文件管理用例矩阵

| ID | 用例 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|
| A-01 | 新建文档 | 侧栏「新建文档」→ 输入中文标题 | 文件生成（slug 正确）、树即时出现、可点开、入最近 | P0 |
| A-02 | 数字标题 | 新建 `1`/`111`/`3.14` | frontmatter `title: "1"`（引号化）、GET 200、可打开 | P0 |
| A-03 | 同名冲突 | 连续新建同名 2 次 | 第二次报错或自动后缀，不覆盖首个 | P0 |
| A-04 | 新建文件夹 | 新建空目录 | 树出现空目录（尾缀 `/` 标记）| P1 |
| A-05 | 重命名文档 | 标题改名（中文→英文） | 文件重命名 + frontmatter title 同步 + 树/最近更新 | P0 |
| A-06 | 重命名冲突 | 改成已存在名 | 409 提示，原名保留 | P0 |
| A-07 | 删除文档 | 删除当前打开文档 | 关闭编辑区、树移除、磁盘删除、快照保留 | P0 |
| A-08 | 嵌套目录 | 5 层嵌套下新建/打开/改名 | 路径与树一致，深路径读写正常 | P1 |
| A-09 | 移动（拖拽/菜单） | 文档移入子目录 | 路径更新、树更新、索引更新、最近可开 | P1 |
| A-10 | 外部新增文件 | 资源管理器放 `外部.md` 进工作区 | 树自动出现（fs 事件），可打开 | P0 |
| A-11 | 外部改名/删除 | 外部改名/删除当前文档 | 提示 + 编辑区对应处理（改名跟随/删除清空）| P0 |
| A-12 | 附件插入 | 拖拽 png/pdf 入编辑器 | 存 `Attachments/`、md 引用正确、往返保真 | P1 |
| A-13 | 导出 | 导出 Markdown/HTML | 内容与 ke-* 语义一致（plain 导出无标记）| P1 |
| A-14 | 快照/历史 | 编辑 3 次后打开历史 | 版本列表可预览/恢复 | P1 |
| A-15 | 工作区切换 | 切到测试工作区再切回 | 树/最近/索引一致，无残留状态 | P0 |
| A-16 | 最近列表 | 打开多篇后看最近 | 排序正确（updated desc）、删除项消失 | P1 |
| A-17 | 索引一致性 | 批量外部改动 20 文件 | `/api/index/rebuild` 后树/最近/搜索一致 | P0 |

## 5. B 域：文件安全用例矩阵

| ID | 用例 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|
| B-01 | 原子写 | 保存时强杀进程（保存中 taskkill）| 原文件完整（无半截），重启后可恢复 | P0 |
| B-02 | 崩溃恢复 | 编辑未保存 → 强杀 → 重启 | 「恢复检查」提示可恢复/丢弃 | P0 |
| B-03 | autosave 落盘 | 编辑后等 autosave 间隔 | 内容落盘（md 校验），状态指示正确 | P0 |
| B-04 | 关窗 flush | 编辑中关窗 | 未决保存 flush 后退出，内容不丢 | P0 |
| B-05 | 外部修改冲突 | 编辑器打开时外部改同文件 | 冲突提示（重新加载/保留我的）| P0 |
| B-06 | 路径穿越 | API 请求 `../../etc/passwd`、`..%2f..` | 400/404 拒绝，不越界读写 | P0 |
| B-07 | 符号链接/Junction | 工作区放指向外部的链接 | 枚举/索引跳过（P1-17 不变量）| P0 |
| B-08 | 非 UTF-8 文件 | 放 GBK 编码 .md 并打开 | 422 + 明确提示，不崩溃 | P0 |
| B-09 | 只读文件 | 设为只读后保存 | 明确错误提示，原内容不损坏 | P1 |
| B-10 | 大文档 | 256KB 文档打开/编辑/保存 | 不卡死（首开提示）、保存完整、往返一致 | P0 |
| B-11 | frontmatter 保留 | 手写注释/嵌套 YAML 后保存正文 | 元信息无损合并（P0-1 合并语义）| P0 |
| B-12 | BOM 处理 | 带 BOM 的 md 打开保存 | 解析正常、正文无 BOM 残留 | P1 |
| B-13 | 备份保留策略 | 连续保存 > 保留数 | 旧快照按策略清理，新快照可用 | P1 |
| B-14 | 单实例 | 二次启动应用 | 重定向到已开窗口（show+focus），不起二实例 | P0 |
| B-15 | 关窗假死回归 | 关闭 → 立刻再启动 | 进程真退出（<6s）、二次启动有窗口（v1.1.6 修复回归）| P0 |
| B-16 | 空/超长文件名 | 新建空名/300 字符名/非法字符 | 拒绝或净化，不产生坏文件 | P1 |
| B-17 | 磁盘写失败模拟 | 目标目录设 ACL 拒绝写 | 报错清晰、不丢原文件 | P2 |

## 6. C 域：编辑体验用例矩阵

| ID | 用例 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|
| C-01 | Markdown 往返保真 | 复合文档（标题/列表/引用/代码/公式/信息块/表格/脚注）保存→重开 | 结构逐字节/语义一致（fidelity 套件 + 手工比对）| P0 |
| C-02 | 列表输入 | `1. ` / `1) ` / `- ` + Enter 空项 | Obsidian 式续列/出列表（v1.1.6 行为）| P0 |
| C-03 | 公式编辑 | Ctrl+N/M → 全屏模态 → 模板/补全/Tab/槽位/Esc | 全链（v1.1.7 验收矩阵复跑）| P0 |
| C-04 | 信息块 | 新建/块级粘贴/空块 Enter 移出/占位符 | v1.1.7 行为稳定 | P0 |
| C-05 | 表格 | 插入/编辑/增删行/往返 | GFM 往返一致 | P1 |
| C-06 | 脚注 | 插入/编号/往返 | 上标 + 底部条目同步 | P1 |
| C-07 | 图片 | 粘贴/拖拽/尺寸/往返 | 附件引用正确 | P1 |
| C-08 | 粘贴外部富文本 | 从浏览器/Word 粘贴 | 结构降级合理、无 `&nbsp;` 类残留 | P1 |
| C-09 | 撤销重做 | 各类操作后 Ctrl+Z/Y | 一致、无残留（含模板插入撤销）| P0 |
| C-10 | 快捷键矩阵 | 全部绑定键（含 Ctrl+S/N/M 等）| 符合文档、无冲突 | P1 |
| C-11 | 主题切换 | 浅/深/跟随系统 | 无闪白、KaTeX/代码块配色正确 | P1 |
| C-12 | 设置持久化 | 改设置→重启 | 全部字段留存（含 mathAutocomplete/透明度）| P0 |
| C-13 | 大文档性能 | 256KB 打开/滚动/输入 | 交互可接受（记录耗时）| P1 |
| C-14 | 启动性能 | 5 次冷启动 | 采样表（基线 1.7s 中位）| P1 |
| C-15 | 标题输入光标 | 拖选/框外松开 | 选区保持（v1.1.5 修复回归）| P1 |

---

## 7. 执行方式

| 层 | 手段 | 产出 |
|---|---|---|
| L1 自动化 | vitest / pytest / cargo / tsc | 基线对比表（通过数、耗时、失败项）|
| L2 接口级 | 脚本化 HTTP（夹具工作区）| A/B 域接口用例逐条结果（状态码/内容校验）|
| L3 GUI 自动化 | CDP 脚本（DOM 断言）| 树/编辑器/模态的结构断言 |
| L4 人工验收 | 主理人实机 | 视觉/手感/交互合理性（图判由主理人）|

破坏性用例（B-01/02/05/09/17）**仅测试工作区**，每例前后快照校验。

## 8. 缺陷分级与报告

- **S0 数据丢失/崩溃**：立即停测、修复、复测（v1.1.6 关窗/数字标题类）
- **S1 功能不可用**：修复后回归
- **S2 体验缺陷**：记录进 v1.1.8 候选
- **S3 建议**：记录

报告模板（每域一节）：`用例ID | 结果(Pass/Fail/Blocked) | 证据(日志/截图/DOM) | 缺陷级别 | 备注`

## 9. 验收标准

1. P0 用例 **100% Pass**（或缺陷已修复并复测）
2. 自动化四套件全绿（vitest 244 / pytest 174 / cargo 13 / tsc 0）
3. 三大域报告齐全 + 缺陷清单（分级）
4. 基线数据（启动耗时、大文档耗时、内存）记录在案

## 10. 交付物

- `docs/test-report-v1.1.8-pre.md`（执行报告：矩阵结果 + 缺陷清单）
- 缺陷修复提交（若有 S0/S1）
- 基线数据表（供 v1.1.8 前后对比）
